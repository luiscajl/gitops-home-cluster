apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lavanda-tmdb.fullname" . }}-test-connection"
  labels:
    {{- include "lavanda-tmdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lavanda-tmdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
