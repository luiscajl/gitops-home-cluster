apiVersion: v1
kind: Service
metadata:
  name: {{ include "lavanda-tmdb.fullname" . }}
  labels:
    {{- include "lavanda-tmdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lavanda-tmdb.selectorLabels" . | nindent 4 }}
