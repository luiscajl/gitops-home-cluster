apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lavanda-transmission.fullname" . }}-test-connection"
  labels:
    {{- include "lavanda-transmission.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lavanda-transmission.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
