apiVersion: v1
kind: Service
metadata:
  name: {{ include "lavanda-transmission.fullname" . }}
  labels:
    {{- include "lavanda-transmission.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lavanda-transmission.selectorLabels" . | nindent 4 }}
