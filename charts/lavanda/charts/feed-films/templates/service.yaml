apiVersion: v1
kind: Service
metadata:
  name: {{ include "feed-films.fullname" . }}
  labels:
    {{- include "feed-films.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "feed-films.selectorLabels" . | nindent 4 }}
