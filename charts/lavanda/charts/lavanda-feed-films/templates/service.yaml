apiVersion: v1
kind: Service
metadata:
  name: {{ include "lavanda-feed-films.fullname" . }}
  labels:
    {{- include "lavanda-feed-films.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lavanda-feed-films.selectorLabels" . | nindent 4 }}
