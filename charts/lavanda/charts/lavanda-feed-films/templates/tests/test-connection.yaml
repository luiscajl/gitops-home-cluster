apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lavanda-feed-films.fullname" . }}-test-connection"
  labels:
    {{- include "lavanda-feed-films.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lavanda-feed-films.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
